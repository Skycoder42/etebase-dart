name: CD - Publish etebase_sync to pub.dev

on:
  push:
    tags:
      - "etebase_sync-v*"

jobs:
  publish:
    name: Publish
    uses: Skycoder42/dart_test_tools/.github/workflows/publish.yml@main
    permissions:
      id-token: write
    with:
      environment: pub-deploy
      tagPrefix: etebase_sync-v
      workingDirectory: packages/etebase_sync
      buildRunner: false
